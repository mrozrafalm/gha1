name: Matrix runs
on:
  push:
    branches:
      - main
jobs:
  thisismatrix:
    strategy:
      matrix:
        nodes: [12, 16, 18]
        os: [ubuntu-latest, windows-latest]
        include:
          - nodes: 14
            npm: true
        exclude:
          - nodes: 12
            os: ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: Deploy stuff
        run: |
         echo "OS: ${{ matrix.os }}"
         echo "NodeJS: ${{ matrix.nodes }}"
      - name: Conditional
        if: ${{ matrix.npm == true }}
        run: |
          echo "OS: ${{ matrix.os }}"
          echo "NodeJS: ${{ matrix.nodes }}"
          echo "NPM: ${{ matrix.npm }}"